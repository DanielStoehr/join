on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: gruppe-223.developerakademie.net
        username: f014bf84
        password: ${{ secrets.da_gruppe223_ftp_pw }}
        local-dir: ./test/
